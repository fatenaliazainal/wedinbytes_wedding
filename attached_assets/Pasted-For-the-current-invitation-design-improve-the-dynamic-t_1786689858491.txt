For the current invitation design, improve the dynamic text positioning specifically for this layout.

IMPORTANT:
Do not modify the background artwork, flowers, butterflies, ribbons, border, texture, colors, or any decorative elements.

The current design has a large empty center area with decorative elements around the edges. Dynamic text should use this central safe area professionally.

Create separate vertical text zones for this design:

1. INVITATION TITLE
   Example:
   "UNDANGAN PERKAHWINAN"

Place it in the upper-middle section.

2. COUPLE NAME GROUP
   Example:
   Nasser
   &
   Alia

Treat Groom + "&" + Bride as ONE group.

The whole group must remain centered horizontally and vertically inside its assigned couple-name zone.

3. DAY
   Example:
   JUMAAT

4. DATE
   Example:
   25 September 2026

5. HASHTAG
   Example:
   #coupleHashtag

Use a structured vertical flow:

TITLE
↓
COUPLE GROUP
↓
DAY
↓
DATE
↓
HASHTAG

Do NOT position each dynamic element independently using arbitrary fixed coordinates.

Instead, define a layout zone for each section with:

* top boundary
* bottom boundary
* maxWidth
* minWidth
* fontSize
* minFontSize
* lineHeight
* spacing
* textAlign

For the couple group:

* Keep it centered.
* Keep Groom, "&", and Bride visually connected.
* Preserve the original font and styling.
* If the names are short, preserve the current visual size and position.
* If the names are longer, reduce font size gradually.
* If necessary, allow controlled wrapping.
* Never allow the couple group to overlap the Day section.

For the entire text stack:

* Keep the title in its own zone.
* Keep the couple names in their own zone.
* Keep the date information in its own zone.
* Keep the hashtag in its own zone.
* Do not allow one section to collide with another section.
* Do not allow text to enter the decorative flower/butterfly areas.

IMPORTANT DESIGN BEHAVIOUR:

Short names:
Nasser
Alia

should look almost exactly like the current design.

Medium names:
Muhammad Hakim
Nur Aisyah

should automatically scale down slightly while staying centered.

Long names:
Muhammad Hakim Bin Abdullah
Nur Aisyah Sofea Binti Ahmad

should scale down further or wrap within the couple-name zone, while maintaining a balanced composition.

Do NOT simply move all elements upward or downward when text becomes longer.

Instead, each section should remain within its designated zone.

The empty central area of this design is intentional and should remain visually spacious.

Also make this configuration reusable:
Each invitation design should be able to define its own zones and spacing values because other designs will have different decorative boundaries and available text areas.

Before coding:

1. Inspect the existing dynamic text rendering system.
2. Identify how the current design stores text coordinates.
3. Identify the preview/rendering component.
4. Implement this as an extension of the existing system rather than creating a completely separate editor.

After implementation, test the design with short, medium, and long couple names and verify that:

* nothing overlaps
* text remains centered
* original short-name appearance is preserved
* decorative elements are not covered
* spacing remains visually balanced
