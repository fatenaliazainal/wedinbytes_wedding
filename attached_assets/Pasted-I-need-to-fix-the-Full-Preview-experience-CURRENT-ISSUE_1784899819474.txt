I need to fix the Full Preview experience.

CURRENT ISSUE:
The live preview shown inside the editor is already displaying the invitation correctly and matches the design I want.

However, when I click the "Full Preview" button, the full preview page displays the invitation differently from the live preview inside the editor.

I want the Full Preview to match the Editor Live Preview exactly.

IMPORTANT:
The Editor Live Preview is the source of truth.

Please inspect the existing code and identify the component used to render the invitation inside the editor preview panel.

The Full Preview should reuse the SAME invitation rendering component, layout structure, styling, design configuration, data mapping, fonts, spacing, sections, navigation, and responsive behavior as the Editor Live Preview.

DO NOT create a separate or duplicate invitation rendering system for Full Preview.

The following should be identical between Editor Live Preview and Full Preview:
- Cover design
- Opening style
- Couple names
- Fonts
- Font sizes
- Colors
- Background images
- Layout
- Spacing
- Invitation text
- Date and location
- Programme
- Prayer
- RSVP
- Wishes
- Footer
- Bottom navigation
- Icons
- Section order
- Mobile layout
- Scroll behavior

The Full Preview should simply render the same invitation experience as the Editor Live Preview, but in a full-page/full-screen layout instead of inside the small preview panel.

ARCHITECTURE REQUIREMENT:

Editor Live Preview:
[Shared Invitation Renderer]
        ↓
    Preview Container

Full Preview:
[Shared Invitation Renderer]
        ↓
    Full Page Container

Both should use the same source of truth.

The current editor preview has a mobile phone-style invitation preview with:
- The invitation content displayed inside the phone frame
- Vertical scrolling
- Bottom fixed navigation containing Music, Calendar, With Love, Location, Contact, and RSVP

The Full Preview should display the same invitation content, same section order, same bottom navigation behavior, and same visual styling as the editor preview.

The only difference should be that Full Preview has a larger/full-screen viewing area.

Please:
1. Inspect the current Editor Live Preview component.
2. Inspect the current Full Preview route/page.
3. Identify why they currently render differently.
4. Refactor the code so both use the same shared invitation renderer.
5. Preserve the existing Editor Live Preview appearance exactly.
6. Make Full Preview match the Editor Live Preview as closely as possible.
7. Only change the outer container, viewport size, and preview mode when displaying Full Preview.

IMPORTANT:
Do not redesign the invitation.
Do not create a new visual design.
Do not change the current Editor Live Preview design.
The goal is to make Full Preview identical to the current Editor Live Preview.

Before making changes, inspect the existing components and explain which component currently renders the Editor Live Preview and which component renders Full Preview.