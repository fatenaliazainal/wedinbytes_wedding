import { useEffect, useState } from "react";
import { useLocation } from "wouter";
import { useAuth } from "@/context/AuthContext";
import SiteHeader, { type SiteNavItem } from "@/components/SiteHeader";
import SharedNavDrawer from "@/components/SharedNavDrawer";
import SiteFooter from "@/components/SiteFooter";
import { ArrowLeft, ExternalLink, ImagePlus, Instagram, Link2, LogOut, Save, User } from "lucide-react";
import { toast } from "sonner";
import { resolveImageUrl } from "@/lib/r2-url";

const BASE = import.meta.env.BASE_URL.replace(/\/$/, "");
const NAV_ITEMS: SiteNavItem[] = [{ label: "HOME", href: "/" }, { label: "CATALOG", href: "/weddingcards/home" }, { label: "PRICE LIST", href: "/pricing" }, { label: "FAQs", href: "/faq" }, { label: "REVIEWS", href: "/reviews" }];
const fields = ["businessName", "businessType", "displayName", "slug", "description", "phone", "whatsapp", "email", "website", "instagram", "facebook", "tiktok", "coverImage", "address", "googleMapsUrl", "businessHours"] as const;
type FormState = Record<typeof fields[number], string>;
const emptyForm = (): FormState => Object.fromEntries(fields.map((field) => [field, ""])) as FormState;

export default function BusinessProfilePage() {
  const { user, loading: authLoading, logout } = useAuth();
  const [, navigate] = useLocation();
  const [navOpen, setNavOpen] = useState(false);
  const [form, setForm] = useState<FormState>(emptyForm());
  const [logoUrl, setLogoUrl] = useState("");
  const [logoUploading, setLogoUploading] = useState(false);
  const [loading, setLoading] = useState(true);
  useEffect(() => { if (!authLoading && (!user || user.role !== "business_account")) navigate(user ? "/dashboard" : "/login"); }, [authLoading, user, navigate]);
  useEffect(() => { if (user?.role !== "business_account") return; fetch(`${BASE}/api/business/me`, { credentials: "include" }).then((res) => res.json()).then((data) => { setLogoUrl(data.logoUrl ?? ""); setForm((current) => Object.fromEntries(fields.map((field) => [field, data[field] ?? current[field]])) as FormState); }).catch(() => toast.error("Unable to load profile.")).finally(() => setLoading(false)); }, [user?.role]);
  if (authLoading || loading || !user || user.role !== "business_account") return <div className="min-h-screen flex items-center justify-center bg-[#faf9f7]"><div className="h-8 w-8 rounded-full border-b-2 border-gray-900 animate-spin" /></div>;
  const save = async (event: React.FormEvent) => { event.preventDefault(); const response = await fetch(`${BASE}/api/business/me`, { method: "PATCH", credentials: "include", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await response.json().catch(() => ({})); if (!response.ok) { toast.error(data.error || "Unable to save profile."); return; } setForm((current) => Object.fromEntries(fields.map((field) => [field, data[field] ?? current[field]])) as FormState); toast.success("Business Profile saved."); };
  const uploadLogo = async (event: React.ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; event.target.value = ""; if (!file) return; if (file.type !== "image/png" || file.size > 2 * 1024 * 1024) { toast.error("Choose a transparent PNG logo no larger than 2 MB."); return; } setLogoUploading(true); try { const body = new FormData(); body.append("file", file); const response = await fetch(`${BASE}/api/business/me/logo`, { method: "POST", credentials: "include", body }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.error || "Unable to upload logo."); setLogoUrl(data.logoUrl ?? ""); toast.success("Logo uploaded."); } catch (error) { toast.error(error instanceof Error ? error.message : "Unable to upload logo."); } finally { setLogoUploading(false); } };
  return <div className="min-h-screen bg-[#fdfdfc] flex flex-col"><SiteHeader navItems={NAV_ITEMS} navOpen={navOpen} setNavOpen={setNavOpen} rightSlot={<><button onClick={() => navigate("/business/dashboard")} className="text-gray-500"><User size={18} /></button><button onClick={async () => { await logout(); navigate("/"); }} className="text-gray-500"><LogOut size={18} /></button></>} /><SharedNavDrawer navItems={NAV_ITEMS} navOpen={navOpen} setNavOpen={setNavOpen} /><main className="flex-1 max-w-4xl w-full mx-auto px-4 sm:px-6 py-8 sm:py-12"><button onClick={() => navigate("/business/dashboard")} className="mb-6 inline-flex items-center gap-2 text-sm text-gray-500"><ArrowLeft size={15} /> Back to dashboard</button><h1 className="text-3xl font-serif text-gray-900">Business Profile</h1><p className="mt-1 mb-8 text-sm text-gray-500">These details will appear on your public profile and in the Collaborations section on our homepage.</p><form onSubmit={save} className="space-y-6"><section className="bg-white border border-gray-200 rounded-xl p-6"><div className="flex items-center gap-2 mb-1"><ImagePlus size={18} className="text-rose-700" /><h2 className="font-semibold text-gray-900">Collaboration profile</h2></div><p className="text-sm text-gray-500 mb-5">Upload a clean transparent logo, then add the name and links guests can use to find your business.</p><div className="flex flex-col sm:flex-row gap-5 items-start"><div className="h-28 w-28 rounded-2xl border border-dashed border-gray-300 bg-[linear-gradient(45deg,#f3f4f6_25%,transparent_25%),linear-gradient(-45deg,#f3f4f6_25%,transparent_25%),linear-gradient(45deg,transparent_75%,#f3f4f6_75%),linear-gradient(-45deg,transparent_75%,#f3f4f6_75%)] bg-[length:16px_16px] bg-[position:0_0,0_8px,8px_-8px,-8px_0px] flex items-center justify-center overflow-hidden">{logoUrl ? <img src={resolveImageUrl(logoUrl)} alt={form.businessName || "Business logo"} className="h-full w-full object-contain p-3" /> : <ImagePlus size={24} className="text-gray-400" />}</div><div><label className="inline-flex items-center gap-2 rounded-lg bg-gray-900 text-white px-4 py-2.5 text-sm font-semibold cursor-pointer">{logoUploading ? "Uploading…" : "Upload logo"}<input type="file" accept="image/png" onChange={uploadLogo} disabled={logoUploading} className="sr-only" /></label><p className="mt-2 text-xs text-gray-500">PNG with transparent background · max 2 MB · max 1600 × 1600 px</p></div></div></section><section className="bg-white border border-gray-200 rounded-xl p-6 grid sm:grid-cols-2 gap-5"><div className="sm:col-span-2 flex items-center gap-2"><Link2 size={18} className="text-rose-700" /><h2 className="font-semibold text-gray-900">Business details and links</h2></div>{fields.map((field) => <label key={field} className={field === "description" ? "sm:col-span-2" : ""}><span className="block text-xs font-semibold uppercase tracking-wider text-gray-500 mb-1">{field.replace(/[A-Z]/g, (letter) => ` ${letter}`).trim()}</span>{field === "description" || field === "address" ? <textarea rows={field === "description" ? 4 : 2} value={form[field]} onChange={(e) => setForm({ ...form, [field]: e.target.value })} className="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm" /> : <input value={form[field]} onChange={(e) => setForm({ ...form, [field]: e.target.value })} className="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm" />}</label>)}</section><button className="inline-flex items-center gap-2 bg-gray-900 text-white rounded-lg px-5 py-2.5 text-sm font-semibold"><Save size={15} /> Save profile</button></form></main><SiteFooter /></div>;
}